- Se ha hecho un test de compilación del algoritmo en un Docker creado sólo con apt-get y se ha ejecutado en euler con buenos resultados. Con estos buenos resultados se ha decidido prescindir del Docker actual del worker, que utiliza una versión de Fortran compilada desde 0, y crear una nueva imagen basada en Ubuntu:20.04 que instala el Fortran desde paquetes y el Node 12.16.3 desde un paquete binario precompilado por el equipo Node. Esta nueva imagen está en **sunnsaas_v1/docker/010/010**. La antigua se ha guardado en el mismo sitio, en **900**. Se precisa de integrar esta imagen en el ciclo completo de SunnSaaS.